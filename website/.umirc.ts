import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'Rc-Grid',
  mode: 'site',
  navs: [
    null,
    { title: 'GitHub', path: 'https://github.com/HighPerformanceComponent/rc-grid' },
  ]
});
